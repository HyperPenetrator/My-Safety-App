function startBatteryMonitoring() {if (!navigator.getBattery) {console.log('Battery API not supported');return;}navigator.getBattery().then(battery => {battery.addEventListener('levelchange', () => {console.log('Battery level:', battery.level*100);if (battery.level <= 0.05) {alert('Battery critical! Sending last location to contacts and police.');sendLocationToContacts();window.open('tel:100');}});});}