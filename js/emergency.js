function triggerEmergencyActions() {window.open('tel:100');window.open('tel:1091');sendLocationToContacts();startRecording();}function sendLocationToContacts() {if (navigator.geolocation) {navigator.geolocation.getCurrentPosition(position => {const lat = position.coords.latitude;const lon = position.coords.longitude;const message = `🚨 EMERGENCY! Location: https://maps.google.com/?q=${lat},${lon}`;console.log(message);alert('Message sent to contacts: ' + message);});} else {alert('Geolocation not supported.');}}function callContact(number) {window.open('tel:' + number);}function startRecording() {console.log('Recording started...');}